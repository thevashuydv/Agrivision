# Preferred for local dev (gitignored via *.local). See https://clerk.com/docs/react/getting-started/quickstart
VITE_CLERK_PUBLISHABLE_KEY=pk_test_...

# Optional: path prefix when serving the built app (e.g. /react/ via FastAPI mount)
# VITE_APP_BASE=/
